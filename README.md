# Local Music Player 2.0 — Kotlin Native

This version replaces the HTML/WebView/JavaScript player with a native Kotlin Android UI and Media3/ExoPlayer playback.

## Build

The included GitHub Actions workflow builds the debug APK in the cloud, so Android Studio is not required on the user's PC.
